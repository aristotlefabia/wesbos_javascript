const p = document.querySelector('p'); // first matching element
console.log(p);

const pall = document.querySelectorAll('p'); // all element
console.log(pall);

const d = document.querySelectorAll('div');
console.log(d);

// div with class of item

const div_with_class = document.querySelector('div.item');
console.log(div_with_class);

const img_with_class = document.querySelector('img.custom');
console.log(img_with_class);

const pizzaList = document.querySelector('.pizza');
console.log(pizzaList);
